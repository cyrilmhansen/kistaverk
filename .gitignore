/repomix-output.xml
**/repomix-output.xml

# ------------------
# 1. ANDROID & GRADLE
# ------------------
**/.gradle/
build/
app/build/
**/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
*.iml
*.class
.gradle-temp/
**/.externalNativeBuild/
**/.cxx/
app/captures/

# Android Profiling
*.hprof
*.aprof

# ------------------
# 2. RUST (The Core)
# ------------------
# The build directory where cargo puts compiled code
rust/target/

# Backup files generated by rustfmt
**/*.rs.bk

# Note: We KEEP Cargo.lock because this is an application, 
# and we want reproducible builds. Do not ignore Cargo.lock.

# ------------------
# 3. JNI / GENERATED LIBS (Critical)
# ------------------
# We must NOT commit the compiled .so files. 
# They are large binaries and are regenerated by 'cargoBuild' every time.
app/src/main/jniLibs/
**/jniLibs/
*.so

# ------------------
# 4. EDITORS (VS Code / Linux)
# ------------------
.vscode/
.history/
*~
app/.idea/caches/
